{
    "name": "api",
    "version": "0.0.0",
    "private": true,
    "scripts": {
        "dev": "wrangler dev",
        "deploy": "node -e \"console.error('Deploy bloqueado: usa deploy:preview o deploy:production de forma explícita'); process.exit(1)\"",
        "deploy:preview": "wrangler deploy --config wrangler.preview.toml",
        "deploy:production": "wrangler deploy --config wrangler.toml"
    },
    "dependencies": {
        "hono": "^4.0.0",
        "resend": "^6.9.2"
    },
    "devDependencies": {
        "@cloudflare/workers-types": "^4.20240208.0",
        "wrangler": "^3.0.0"
    }
}
